pipeline {
    agent any


   tools {
       maven 'maven3'
   }

  triggers {
    githubPush()
  }
  
    stages {
        stage('git checkout') {
            steps {
                git ''
            }
        }
        
                stage ('maven build war file'){
            steps {
                sh 'mvn clean package'
            }
        }
    }
}
